'use strict';

const userRoles = ['moderator', 'guest'];
const retrospectiveSteps = ['add-items', 'group-items', 'vote-items', 'action-items', 'report'];
const teamName = 'DEVINT22';
const projectName = 'Retrospective';
const secretTokenKey = 'RetrospectiveTool-DEVINT22-secretKey';

module.exports = {
  retrospectiveSteps, teamName, projectName, userRoles, secretTokenKey
};
